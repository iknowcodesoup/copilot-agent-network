---
topic: pythonapi chat agent: build the outbound half of the AG-UI tool loop so the model can call browser-registered tools
updated: 2026-08-13T15:57
---

- (direction) User asked to convert the spec at specs/004-agui-frontend-tool-loop/README.md into BMAD SPEC kernel format, full fidelity into companions.
- (note) The chat agent cannot call a tool. The docstring in core/chat_agent.py (lines 8-12) says so, and the code proves it: the completion call (lines 56-60) passes no tools= argument, and the generator never emits a ToolCall* event. So every tool the browser registers is invisible to the model.
- (note) What already works: _to_openai_messages (lines 80-113) handles role == tool and copies tool_calls onto the assistant message - the inbound half is built. routes/agent.py needs no change, it streams whatever the generator yields.
- (note) This spec changes no data model and no screen, so specs 001-003 (video-scoped ingestion, multi-voice data model, videos/voices views) do not affect it and it does not affect them.
- (capability) CAP-1 intent: the chat agent forwards the AG-UI tool definitions it receives to the model, so a tool the browser registers is visible to the model. success: a request carrying agent_input.tools maps them onto the OpenAI tools= shape with tool_choice=auto; a request carrying no tools omits both arguments and behaves exactly as today.
- (capability) CAP-2 intent: when the model calls a tool, the agent emits a complete, correctly ordered AG-UI tool-call event sequence, reassembled from the OpenAI stream's per-index fragments. success: a tool call emits ToolCallStartEvent, then one or more ToolCallArgsEvent in order, then ToolCallEndEvent, and the reassembled argument text equals what the model sent.
- (capability) CAP-3 intent: after emitting tool calls, the run ends rather than waiting, because a frontend tool runs in the browser and the server cannot run it. success: RunFinishedEvent is emitted after the tool calls; the server issues no further action for that run; the browser is expected to POST a fresh RunAgentInput with the tool result appended to continue.
- (capability) CAP-4 intent: a text message only opens on screen when text actually arrives, so a tool-only turn does not leave an empty message in the transcript. success: TextMessageStartEvent fires on the first non-empty content delta, not before; a turn carrying both text and a tool call emits both, with exactly one text message opened.
- (capability) CAP-5 intent: a model that keeps calling tools without producing a final answer is stopped after a configurable number of turns, so the loop cannot run away. success: counting assistant turns in agent_input.messages that end in a tool call, the server answers with text instead of another tool call once the count exceeds LLM_MAX_TOOL_TURNS; the count is derived from the message list so the server stays stateless.
- (constraint) LLM_MAX_TOOL_TURNS is a Settings field in config.py with a real default - not a bare None, per project convention that a missing setting must fail loudly at the config boundary, not silently at the call site.
- (constraint) The server never waits for a tool result. CopilotKit runs frontend tools in the browser; the AG-UI contract for frontend tools is that the run ends and a new run starts, keeping each run independent and the server stateless.
- (constraint) The module docstring in chat_agent.py currently states the agent cannot call tools - it must be rewritten as part of this change, since it will state the opposite of the new behavior otherwise.
- (non-goal) This spec does not touch routes/agent.py; it already streams whatever the generator yields and needs no change.
- (non-goal) This spec does not change any data model or screen; specs 001 through 003 (ingestion, data model, frontend views) are unaffected by it and do not affect it.
- (event) Self-validate pass 1 (coherence): PASS. 5 capabilities each carry intent and success; 3 constraints each bend design (config default discipline, stateless server, docstring accuracy); 2 non-goals stated; success signal (register one throwaway frontend tool, ask for it, read the answer) is demonstrable end to end. OpenAI-shape mapping detail and ordered steps routed to implementation-plan.md per rule 2.
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing claim in the source README (what already works, what is missing, the four-part target design, why the client resumes, ordered plan, test scenarios, cross-spec note that 005 depends on this) lands in SPEC.md or a companion (brownfield.md, implementation-plan.md, verification.md). No wrapper-only content dropped.
