FROM node:22-alpine
WORKDIR /app
RUN npm install -g npm@11.7.0
COPY package.json package-lock.json ./
RUN npm ci

COPY . .

ENV NODE_ENV=development
ENV PORT=3000

WORKDIR /app/apps/agentic-executor
EXPOSE 3000
CMD ["npx", "next", "dev", "--hostname", "0.0.0.0", "--port", "3000"]